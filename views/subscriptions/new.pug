extends ../layouts/main.pug

block content
  .column.is-6.is-offset-3
    .box
      h1.title Nueva Suscripción
      form(action=`/subscriptions/create${sim.query}` method="POST")
        .field
          label.label Plan
          .control
            .select.is-fullwidth
              select(name="detail" required)
                option(value="" disabled selected) Seleccioná un plan
                option(value="Basico") Básico
                option(value="Profesional") Profesional
                option(value="Enterprise") Enterprise
                option(value="Demo Comercio") Demo Comercio

        .field
          label.label Monto
          .control
            input.input(type="number" name="amount" placeholder="50.000" required)

        .field
          label.label Tienda
          .control
            div.select.is-fullwidth
              select(name="storeId")
                option(value="") Seleccioná una tienda
                each store in stores
                  option(value=`${store._id}`)= store.name

        .buttons
          button.button.is-primary(type="submit") Guardar
          a.button.is-light(href=`/subscriptions${sim.query}`) Cancelar


