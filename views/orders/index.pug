extends ../layouts/main

block content
  h1.title Gestion de Ordenes

  .buttons
    a.button.is-primary(href=`/orders/new${sim.query}`) Nueva Orden

  if view === 'form'
    include create
  else
    include list
